{{ form_start(available_routes_form_ajax, { 'attr' : {'novalidate': 'novalidate', 'role' : 'form', 'id': 'available-routes-ajax'|default('form')} }) }}
{{ form_end(available_routes_form_ajax) }}

<style>
    #available_routes {
        position: absolute;
        display: block;
        top: 0px;
        left: 0px;
        width: 100%;
        height: 100%;
        background-color: yellow;
    }
</style>

<div id="available_routes">
    
</div>

<script>
    window.onload = availableRoutes_33();

    function availableRoutes_33()
    {
        var form = $('#available-routes-ajax');
        var url = form.attr('action');

        $.post(url, function (data)
        {
{#            getNodes(data);#}
            var parameter_1 = document.getElementById("available_routes");
            drawSaleDetail(data, parameter_1);
        }).fail(function () {
            alert('ERROR available-routes-ajax');
        });
    }
    
    function getNodes(data)
    {
        var longitud = data.length;

        var level = new Array();
        
        for (i = 0; i < longitud; i++)
        {
            var trajectory = data[i].trajectory;
            
            var longh = data[i].trajectory.length;
            
            var k = 0;
            for (j=longh-1; j>=0; j--)
            {
                alert("bhgfvds "+ data[i].trajectory[j]);
                {#var cdscd = data[i].trajectory[j];
                alert("cdscd "+cdscd);#}
                level[i][k] = data[i].trajectory[j];
                k++;
            }
            
            drawTrajectory();
            
            {#var k = 0;
            while(data[i].trajectory[k])
            { 
                data[i].trajectory[k];
                k++;
            }#}
        }
        
        {#while(data[i].trajectory[k])
        {
            product_details.value += 
            data[i].trajectory[k] + " - ";
            k++;
        }#}
    }
    
    function drawTrajectory()
    {
        var saleDetail_route = document.createElement("div");
        saleDetail_route.id = "saleDetail_route" + saleDetail_id_1;
        saleDetail_route.className = "panel22";

        document.getElementById("available_routes").appendChild(saleDetail_id);
    }
</script>